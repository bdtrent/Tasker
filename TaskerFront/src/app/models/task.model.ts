export class Task {
    id?: any;
    // TODO: Do we need this?
    listId?: any;
    title?: string;
    description?: string;
    assign_date?: string;
    due_date?: string;
    completed?: boolean;
}
  
